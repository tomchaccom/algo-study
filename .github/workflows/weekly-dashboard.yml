name: Weekly Dashboard

on:
  push:
    branches:
      - main
  schedule:
    - cron: '59 14 * * 0'
  workflow_dispatch:

jobs:
  dashboard:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Generate Weekly Stats
        id: stats
        uses: actions/github-script@v6
        with:
          script: |
            const org = context.repo.owner;
            const repo = context.repo.repo;

            const { data: members } = await github.rest.orgs.listMembers({
              org: org,
              per_page: 100
            });

            const memberLogins = members.map(m => m.login);

            const now = new Date();
            const kstOffset = 9 * 60 * 60 * 1000;
            const kstNow = new Date(now.getTime() + kstOffset);

            const day = kstNow.getUTCDay();
            const diff = day === 0 ? 6 : day - 1;

            const mondayKST = new Date(kstNow);
            mondayKST.setUTCDate(kstNow.getUTCDate() - diff);
            mondayKST.setUTCHours(0, 0, 0, 0);

            const mondayUTC = new Date(mondayKST.getTime() - kstOffset);

            const { data: pulls } = await github.rest.pulls.list({
              owner: org,
              repo: repo,
              state: "closed",
              per_page: 100
            });

            const weekly = pulls.filter(pr =>
              pr.merged_at && new Date(pr.merged_at) >= mondayUTC
            );

            const counts = {};
            memberLogins.forEach(user => counts[user] = 0);

            weekly.forEach(pr => {
              if (counts.hasOwnProperty(pr.user.login)) {
                counts[pr.user.login]++;
              }
            });

            let table = "";
            table += `📅 기준: ${mondayKST.toISOString().slice(0,10)} (월요일 00:00 KST)\n\n`;
            table += "| Member | Count | Status |\n";
            table += "|--------|-------|--------|\n";

            for (const user of memberLogins) {
              const count = counts[user];
              const status = count >= 5 ? "✅" : "❌";
              table += `| ${user} | ${count} | ${status} |\n`;
            }

            require('fs').writeFileSync('dashboard.md', table);

      - name: Inject into README
        run: |
          awk '
          FNR==NR {dashboard=$0"\n"; next}
          /<!-- WEEKLY_DASHBOARD_START -->/ {
            print;
            print "";
            while ((getline line < "dashboard.md") > 0) print line;
            skip=1;
            next;
          }
          /<!-- WEEKLY_DASHBOARD_END -->/ { skip=0 }
          !skip
          ' dashboard.md README.md > temp.md

          mv temp.md README.md

      - name: Commit changes
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add README.md
          git commit -m "📊 weekly dashboard update" || echo "No changes"
          git push

